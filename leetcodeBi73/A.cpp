#include<iostream>
#include<sstream>

#define mx 10000
#define mod 1000007
#define ll long long

using namespace std;


int main(int argc, char const *argv[])
{
    char a ='1';
    auto b= isdigit(a);
    auto low= tolower(a);

    string s="11",s2="22";
    auto in= stoi(s);
    auto l = stol(s);
    stringstream ss(s);

    string s3="1 1 1";
    auto vi=atoi(s.data());
    auto cmp=s.compare(s2);
    int si;
    ss>>si;

    ostringstream os;

    int* c= new int[1];
    int cc=c[0];
    
    
    return 0;
}
